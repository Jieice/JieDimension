---
import { Icon } from "astro-icon/components";
import MainGridLayout from "@/layouts/MainGridLayout.astro";

const updatedDate = "2026 年 7 月 22 日";
const email = "3348149202@qq.com";

const policySections = [
	{ id: "collection", number: "01", label: "收集范围" },
	{ id: "usage", number: "02", label: "使用方式" },
	{ id: "storage", number: "03", label: "存储与安全" },
	{ id: "third-party", number: "04", label: "第三方服务" },
	{ id: "minors", number: "05", label: "未成年人保护" },
	{ id: "rights", number: "06", label: "您的权利" },
	{ id: "contact", number: "07", label: "联系我们" },
	{ id: "updates", number: "08", label: "政策更新" },
];
---

<MainGridLayout
	title="《界维守卫》隐私政策"
	description="界维守卫（Dimension Defender）隐私政策，了解游戏的数据处理方式、存储位置和用户权利。"
	fullWidth={true}
	visibleTitle={true}
	hideBanner={true}
>
	<div class="policy-page">
		<header class="policy-hero" aria-labelledby="policy-title">
			<img
				class="policy-hero-image"
				src="/assets/images/dimension-defender/hero.webp"
				alt="界维守卫战斗场景"
				width="1920"
				height="1080"
				fetchpriority="high"
			/>
			<div class="policy-hero-shade" aria-hidden="true"></div>
			<div class="policy-hero-content">
				<a class="policy-back-link" href="/">
					<Icon name="material-symbols:arrow-back-rounded" aria-hidden="true" />
					<span>返回主页</span>
				</a>
				<div class="policy-brand-row">
					<img
						class="policy-game-icon"
						src="/assets/images/dimension-defender/icon.webp"
						alt="界维守卫图标"
						width="112"
						height="112"
					/>
					<div>
						<p class="policy-eyebrow">JIEDIMENSION STUDIO · GAME PRIVACY</p>
						<h1 id="policy-title">《界维守卫》隐私政策</h1>
						<p class="policy-hero-summary">
							这是一款本地优先的单机游戏。本页面说明当前版本如何处理游戏数据，以及您可以如何管理这些数据。
						</p>
					</div>
				</div>
				<div class="policy-hero-meta">
					<span>更新日期：{updatedDate}</span>
					<span>适用版本：当前 Android 发布版</span>
					<button type="button" class="policy-print-button" data-print-policy>
						<Icon name="material-symbols:print-outline" aria-hidden="true" />
						<span>打印政策</span>
					</button>
				</div>
			</div>
		</header>

		<section class="policy-lead" aria-label="政策摘要">
			<div>
				<p class="policy-section-kicker">先看结论</p>
				<h2>不注册、不联网，数据留在你的设备上</h2>
			</div>
			<p>
				根据当前代码和 Android 导出配置，本游戏不要求注册账号，不接入广告、统计分析、云存档、支付或在线反作弊服务，也不主动向我们上传游戏数据。游戏进度和设置只保存在设备的应用私有空间。
			</p>
		</section>

		<div class="policy-document-layout">
			<nav class="policy-index" aria-label="政策目录">
				<p class="policy-index-title">目录</p>
				<ol>
					{policySections.map((section) => (
						<li>
							<a href={`#${section.id}`} data-policy-link={section.id}>
								<span>{section.number}</span>
								{section.label}
							</a>
						</li>
					))}
				</ol>
			</nav>

			<article class="policy-document">
				<section id="collection" class="policy-section">
					<div class="policy-section-heading">
						<span>01</span>
						<h2>我们收集的信息</h2>
					</div>
					<p>
						我们遵循最小必要原则。当前版本不会要求您创建账号，也不会主动收集姓名、手机号、通讯录、精确位置、相机或麦克风内容。
					</p>
					<ul class="policy-list">
						<li>
							<strong>游戏存档与设置</strong>
							<span>游戏进度、解锁内容、成就和偏好设置会保存在您的设备本地，用于继续游戏和恢复设置。</span>
						</li>
						<li>
							<strong>运行所需的系统信息</strong>
							<span>游戏运行时会读取屏幕尺寸、方向等系统信息以完成渲染和适配。这些信息只在本地运行流程中使用，不会主动传输给我们。</span>
						</li>
						<li>
							<strong>本地诊断信息</strong>
							<span>Godot 引擎或系统可能在设备本地生成运行日志、错误信息或性能诊断信息，用于排查异常；当前版本不会自动上传这些日志。</span>
						</li>
						<li>
							<strong>您主动提交的反馈</strong>
							<span>当您通过邮件或 TapTap 联系我们时，我们会处理您主动提供的邮箱、问题描述、截图或设备信息，仅用于回复和问题处理。</span>
						</li>
					</ul>
					<div class="policy-note policy-note-cyan">
						<Icon name="material-symbols:info-outline" aria-hidden="true" />
						<p>当前版本不主动收集 Android ID、IP 地址或网络类型，也不请求互联网权限；若未来功能发生变化，我们会先更新本政策。</p>
					</div>
				</section>

				<section id="usage" class="policy-section">
					<div class="policy-section-heading">
						<span>02</span>
						<h2>我们如何使用信息</h2>
					</div>
					<p>我们仅在实现下列目的所需范围内使用相关信息：</p>
					<ul class="policy-check-list">
						<li>提供并维护游戏的核心功能，包括存档、设置和进度恢复。</li>
						<li>在不同屏幕和设备上完成界面适配，改善运行稳定性。</li>
						<li>根据您主动提交的内容回复咨询、处理反馈和定位问题。</li>
						<li>识别和修复本地运行错误，不用于广告画像或跨服务跟踪。</li>
					</ul>
				</section>

				<section id="storage" class="policy-section">
					<div class="policy-section-heading">
						<span>03</span>
						<h2>信息存储与安全</h2>
					</div>
					<p>
						游戏存档写入 Android 应用私有空间，并由游戏使用加密配置文件保存。我们不会建立用于保存游戏进度的云端账户或服务器数据库。
					</p>
					<p>
						您可以在游戏设置中重置本地数据，或通过卸载游戏删除应用私有空间内的存档和设置。Android 系统的备份、迁移或设备厂商服务可能按照其自身规则处理应用数据，相关行为不由我们控制。
					</p>
					<p>
						如果您通过邮件或平台反馈向我们提交信息，我们会在完成回复、问题处理或争议解决所需的期限内保留，并在不再需要时删除或匿名化。
					</p>
				</section>

				<section id="third-party" class="policy-section">
					<div class="policy-section-heading">
						<span>04</span>
						<h2>第三方服务与组件</h2>
					</div>
					<p>
						本游戏使用 Godot 4.7 开源引擎，以及 AndroidX、Material Components 等用于运行和界面呈现的开源组件。它们随应用安装在设备上，不代表接入了广告、统计或数据分析 SDK。
					</p>
					<p>
						当前版本不接入第三方广告、用户行为分析、崩溃上报、登录、支付或云存档服务。您在 TapTap 上下载、评论或私信时，TapTap 可能按照其自身隐私政策处理信息。
					</p>
				</section>

				<section id="minors" class="policy-section">
					<div class="policy-section-heading">
						<span>05</span>
						<h2>未成年人保护</h2>
					</div>
					<p>
						本游戏不以收集未成年人个人信息为目的，也不要求未成年人提供身份信息或创建账号。未成年人应在监护人陪同和指导下阅读本政策、安装和使用本游戏。
					</p>
					<p>
						如果监护人发现未成年人通过邮件或平台向我们提交了个人信息，可以联系我们申请查询、删除或停止处理相关内容。
					</p>
				</section>

				<section id="rights" class="policy-section">
					<div class="policy-section-heading">
						<span>06</span>
						<h2>您的权利</h2>
					</div>
					<p>在适用法律允许的范围内，您可以：</p>
					<ul class="policy-check-list">
						<li>通过游戏设置或卸载游戏，管理和删除设备上的本地存档。</li>
						<li>就您主动提交给我们的反馈信息，申请查询、更正、删除或限制处理。</li>
						<li>停止使用本游戏，或就隐私政策内容向我们提出意见和建议。</li>
					</ul>
					<p>我们可能需要核实请求人与反馈记录的对应关系，并在合理期限内回复。</p>
				</section>

				<section id="contact" class="policy-section policy-contact-section">
					<div class="policy-section-heading">
						<span>07</span>
						<h2>联系我们</h2>
					</div>
					<p>如果您对本政策、数据处理或未成年人信息保护有疑问，请联系：</p>
					<div class="policy-contact-row">
						<a class="policy-contact-link" href={`mailto:${email}`}>
							<Icon name="material-symbols:mail-outline" aria-hidden="true" />
							<span>{email}</span>
						</a>
						<span class="policy-contact-detail">界维互动工作室（JieDimension Studio）</span>
					</div>
					<p class="policy-contact-note">您也可以通过 TapTap《界维守卫》评论区或私信联系我们。</p>
				</section>

				<section id="updates" class="policy-section policy-last-section">
					<div class="policy-section-heading">
						<span>08</span>
						<h2>政策更新</h2>
					</div>
					<p>
						我们可能因功能、法律或服务变化修订本政策。更新后的版本会在本页面公布，并在游戏内隐私政策入口同步；如变化涉及新的个人信息处理，我们会按照适用法律要求取得您的同意。
					</p>
					<div class="policy-effective-row">
						<span>当前版本生效日期</span>
						<strong>{updatedDate}</strong>
					</div>
				</section>
			</article>
		</div>

		<footer class="policy-page-footer">
			<a href="/">JieDimension</a>
			<span aria-hidden="true">/</span>
			<span>《界维守卫》隐私政策</span>
			<span aria-hidden="true">/</span>
			<span>更新于 {updatedDate}</span>
		</footer>
	</div>
</MainGridLayout>

<script>
	const normalizePolicyLayout = () => {
		const pageWrapper = document.querySelector<HTMLElement>(".page-no-banner");
		const mainGrid = document.getElementById("main-grid");
		if (!pageWrapper || !mainGrid) return;

		pageWrapper.style.setProperty("top", "5.5rem", "important");
		mainGrid.style.setProperty("transform", "none", "important");
		document.getElementById("top-row")?.classList.add("page-no-banner-navbar");
	};

	normalizePolicyLayout();
	requestAnimationFrame(normalizePolicyLayout);
	window.setTimeout(normalizePolicyLayout, 250);
	document.addEventListener("astro:page-load", normalizePolicyLayout);

	const printButton = document.querySelector<HTMLButtonElement>("[data-print-policy]");
	printButton?.addEventListener("click", () => window.print());

	const policyLinks = Array.from(document.querySelectorAll<HTMLAnchorElement>("[data-policy-link]"));
	const policySections = policyLinks
		.map((link) => document.getElementById(link.dataset.policyLink || ""))
		.filter((section): section is HTMLElement => section !== null);

	if ("IntersectionObserver" in window && policySections.length > 0) {
		const observer = new IntersectionObserver(
			(entries) => {
				for (const entry of entries) {
					if (!entry.isIntersecting) continue;
					for (const link of policyLinks) {
						link.classList.toggle("is-active", link.dataset.policyLink === entry.target.id);
					}
				}
			},
			{ rootMargin: "-18% 0px -68% 0px", threshold: 0 },
		);
		policySections.forEach((section) => observer.observe(section));
	}
</script>

<style>
	.policy-page {
		--policy-ink: #182033;
		--policy-muted: #566172;
		--policy-line: rgb(24 32 51 / 0.13);
		--policy-cyan: #63e3ff;
		--policy-violet: #a78bfa;
		--policy-green: #7de2b2;
		width: 100%;
		max-width: 78rem;
		margin: 0 auto;
		padding: 0 0 1rem;
		overflow-x: clip;
		color: var(--policy-ink);
	}

	:global(.dark) .policy-page {
		--policy-ink: #f1f5fb;
		--policy-muted: #aeb7c3;
		--policy-line: rgb(255 255 255 / 0.13);
	}

	.policy-hero {
		position: relative;
		isolation: isolate;
		min-height: 27rem;
		display: flex;
		align-items: flex-end;
		overflow: hidden;
		border: 1px solid rgb(255 255 255 / 0.16);
		border-radius: 0.75rem;
		background: #090b20;
		box-shadow: 0 1.5rem 4rem rgb(4 6 25 / 0.2);
	}

	.policy-hero-image,
	.policy-hero-shade {
		position: absolute;
		inset: 0;
		width: 100%;
		height: 100%;
	}

	.policy-hero-image {
		z-index: -2;
		object-fit: cover;
		object-position: center 58%;
	}

	.policy-hero-shade {
		z-index: -1;
		background: rgb(5 7 28 / 0.66);
	}

	.policy-hero-content {
		width: 100%;
		padding: 1.5rem clamp(1.25rem, 4vw, 3.5rem) 2rem;
		color: #fff;
	}

	.policy-back-link,
	.policy-print-button {
		display: inline-flex;
		align-items: center;
		gap: 0.45rem;
		font-size: 0.875rem;
		font-weight: 600;
		line-height: 1;
	}

	.policy-back-link {
		margin-bottom: 1.7rem;
		color: rgb(255 255 255 / 0.82);
		text-decoration: none;
	}

	.policy-back-link:hover,
	.policy-back-link:focus-visible {
		color: var(--policy-cyan);
	}

	.policy-back-link :global(svg),
	.policy-print-button :global(svg),
	.policy-contact-link :global(svg) {
		width: 1.1rem;
		height: 1.1rem;
		flex: 0 0 auto;
	}

	.policy-brand-row {
		display: flex;
		align-items: center;
		gap: 1.25rem;
		max-width: 58rem;
	}

	.policy-brand-row > div {
		min-width: 0;
	}

	.policy-game-icon {
		width: 7rem;
		height: 7rem;
		flex: 0 0 7rem;
		border: 1px solid rgb(255 255 255 / 0.38);
		border-radius: 1rem;
		object-fit: cover;
		box-shadow: 0 0.75rem 1.75rem rgb(0 0 0 / 0.32);
	}

	.policy-eyebrow,
	.policy-section-kicker,
	.policy-index-title {
		margin: 0 0 0.55rem;
		font-size: 0.72rem;
		font-weight: 700;
		letter-spacing: 0.16em;
		text-transform: uppercase;
	}

	.policy-eyebrow {
		color: var(--policy-cyan);
	}

	.policy-hero h1 {
		margin: 0;
		font-size: 2.55rem;
		font-weight: 750;
		line-height: 1.12;
		letter-spacing: 0;
	}

	.policy-hero-summary {
		max-width: 43rem;
		margin: 0.8rem 0 0;
		color: rgb(255 255 255 / 0.82);
		font-size: 1rem;
		line-height: 1.8;
		overflow-wrap: anywhere;
	}

	.policy-hero-meta {
		display: flex;
		align-items: center;
		flex-wrap: wrap;
		gap: 0.7rem 1.1rem;
		margin-top: 1.4rem;
		color: rgb(255 255 255 / 0.72);
		font-size: 0.82rem;
	}

	.policy-hero-meta > span {
		padding-right: 1.1rem;
		border-right: 1px solid rgb(255 255 255 / 0.26);
	}

	.policy-print-button {
		padding: 0.62rem 0.8rem;
		border: 1px solid rgb(255 255 255 / 0.32);
		border-radius: 0.45rem;
		background: rgb(255 255 255 / 0.1);
		color: #fff;
		cursor: pointer;
	}

	.policy-print-button:hover,
	.policy-print-button:focus-visible {
		border-color: var(--policy-cyan);
		background: rgb(99 227 255 / 0.16);
	}

	.policy-lead {
		display: grid;
		grid-template-columns: minmax(13rem, 0.72fr) minmax(0, 1.5fr);
		gap: 2.5rem;
		align-items: start;
		margin: 1.25rem 0 0;
		padding: 1.5rem 0 1.65rem;
		border-top: 1px solid var(--policy-line);
		border-bottom: 1px solid var(--policy-line);
	}

	.policy-section-kicker {
		color: var(--policy-violet);
	}

	.policy-lead h2 {
		margin: 0;
		font-size: 1.4rem;
		line-height: 1.35;
		letter-spacing: 0;
	}

	.policy-lead > p {
		margin: 0;
		color: var(--policy-muted);
		font-size: 0.98rem;
		line-height: 1.9;
	}

	.policy-document-layout {
		display: grid;
		grid-template-columns: 12rem minmax(0, 1fr);
		gap: clamp(1.5rem, 4vw, 4rem);
		align-items: start;
		margin-top: 2rem;
	}

	.policy-index {
		position: sticky;
		top: 5.75rem;
		padding: 0.25rem 0 0.25rem 0.25rem;
	}

	.policy-index-title {
		color: var(--policy-muted);
	}

	.policy-index ol {
		margin: 0;
		padding: 0;
		list-style: none;
	}

	.policy-index li + li {
		margin-top: 0.15rem;
	}

	.policy-index a {
		display: flex;
		align-items: baseline;
		gap: 0.65rem;
		padding: 0.42rem 0.55rem;
		border-left: 2px solid transparent;
		color: var(--policy-muted);
		font-size: 0.84rem;
		line-height: 1.35;
		text-decoration: none;
		transition: color 150ms ease, border-color 150ms ease, background-color 150ms ease;
	}

	.policy-index a span {
		color: var(--policy-violet);
		font-variant-numeric: tabular-nums;
		font-size: 0.72rem;
	}

	.policy-index a:hover,
	.policy-index a:focus-visible,
	.policy-index a.is-active {
		border-left-color: var(--policy-cyan);
		background: color-mix(in srgb, var(--policy-cyan) 9%, transparent);
		color: var(--policy-ink);
	}

	.policy-document {
		min-width: 0;
		padding: 0 0.25rem;
	}

	.policy-section {
		padding: 0 0 2.4rem;
		scroll-margin-top: 6rem;
	}

	.policy-section + .policy-section {
		padding-top: 2.25rem;
		border-top: 1px solid var(--policy-line);
	}

	.policy-last-section {
		padding-bottom: 0;
	}

	.policy-section-heading {
		display: flex;
		align-items: baseline;
		gap: 0.85rem;
		margin-bottom: 0.85rem;
	}

	.policy-section-heading > span {
		color: var(--policy-cyan);
		font-size: 0.76rem;
		font-weight: 700;
		font-variant-numeric: tabular-nums;
		letter-spacing: 0.08em;
	}

	.policy-section h2 {
		margin: 0;
		font-size: 1.42rem;
		line-height: 1.35;
		letter-spacing: 0;
	}

	.policy-section p {
		margin: 0.7rem 0 0;
		color: var(--policy-muted);
		font-size: 0.98rem;
		line-height: 1.9;
		overflow-wrap: anywhere;
	}

	.policy-list,
	.policy-check-list {
		margin: 1.15rem 0 0;
		padding: 0;
		list-style: none;
	}

	.policy-list {
		border-top: 1px solid var(--policy-line);
	}

	.policy-list li {
		display: grid;
		grid-template-columns: minmax(9rem, 0.38fr) minmax(0, 1fr);
		gap: 1.25rem;
		padding: 0.95rem 0;
		border-bottom: 1px solid var(--policy-line);
	}

	.policy-list strong {
		font-size: 0.95rem;
		line-height: 1.6;
	}

	.policy-list span,
	.policy-check-list li {
		color: var(--policy-muted);
		font-size: 0.94rem;
		line-height: 1.8;
	}

	.policy-check-list li {
		position: relative;
		padding-left: 1.35rem;
	}

	.policy-check-list li + li {
		margin-top: 0.45rem;
	}

	.policy-check-list li::before {
		position: absolute;
		left: 0;
		top: 0.58rem;
		width: 0.42rem;
		height: 0.42rem;
		border-radius: 50%;
		background: var(--policy-green);
		content: "";
	}

	.policy-note {
		display: flex;
		align-items: flex-start;
		gap: 0.7rem;
		margin-top: 1.25rem;
		padding: 0.85rem 1rem;
		border-left: 3px solid var(--policy-cyan);
		background: color-mix(in srgb, var(--policy-cyan) 8%, transparent);
	}

	.policy-note :global(svg) {
		width: 1.2rem;
		height: 1.2rem;
		flex: 0 0 auto;
		margin-top: 0.12rem;
		color: var(--policy-cyan);
	}

	.policy-note p {
		margin: 0;
		font-size: 0.9rem;
		line-height: 1.75;
	}

	.policy-contact-row {
		display: flex;
		align-items: center;
		flex-wrap: wrap;
		gap: 0.9rem 1.25rem;
		margin-top: 1.15rem;
	}

	.policy-contact-link {
		display: inline-flex;
		align-items: center;
		gap: 0.55rem;
		padding: 0.7rem 0.9rem;
		border: 1px solid color-mix(in srgb, var(--policy-cyan) 55%, var(--policy-line));
		border-radius: 0.45rem;
		color: var(--policy-ink);
		font-weight: 650;
		text-decoration: none;
	}

	.policy-contact-link:hover,
	.policy-contact-link:focus-visible {
		background: color-mix(in srgb, var(--policy-cyan) 10%, transparent);
		color: var(--policy-cyan);
	}

	.policy-contact-detail,
	.policy-contact-note {
		color: var(--policy-muted);
		font-size: 0.9rem;
	}

	.policy-contact-note {
		margin-top: 0.9rem !important;
	}

	.policy-effective-row {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1rem;
		margin-top: 1.35rem;
		padding-top: 0.9rem;
		border-top: 1px solid var(--policy-line);
		color: var(--policy-muted);
		font-size: 0.86rem;
	}

	.policy-effective-row strong {
		color: var(--policy-green);
		font-weight: 650;
	}

	.policy-page-footer {
		display: flex;
		align-items: center;
		justify-content: center;
		flex-wrap: wrap;
		gap: 0.45rem 0.7rem;
		margin-top: 2.5rem;
		padding-top: 1rem;
		border-top: 1px dashed var(--policy-line);
		color: var(--policy-muted);
		font-size: 0.78rem;
	}

	.policy-page-footer a {
		color: var(--policy-ink);
		font-weight: 650;
		text-decoration: none;
	}

	.policy-page-footer a:hover,
	.policy-page-footer a:focus-visible {
		color: var(--policy-cyan);
	}

	@media (max-width: 900px) {
		.policy-document-layout {
			grid-template-columns: 1fr;
			gap: 1.25rem;
		}

		.policy-index {
			position: static;
			overflow-x: auto;
			padding: 0 0 0.25rem;
			border-bottom: 1px solid var(--policy-line);
		}

		.policy-index-title {
			margin-bottom: 0.35rem;
		}

		.policy-index ol {
			display: flex;
			min-width: max-content;
			gap: 0.35rem;
		}

		.policy-index li + li {
			margin-top: 0;
		}

		.policy-index a {
			border-left: 0;
			border-bottom: 2px solid transparent;
			padding: 0.45rem 0.55rem;
		}

		.policy-index a:hover,
		.policy-index a:focus-visible,
		.policy-index a.is-active {
			border-left-color: transparent;
			border-bottom-color: var(--policy-cyan);
		}
	}

	@media (max-width: 640px) {
		:global(.page-no-banner-navbar #search-switch),
		:global(.page-no-banner-navbar #music-player-switch),
		:global(.page-no-banner-navbar #bg-player-toggle),
		:global(.page-no-banner-navbar #display-settings-switch),
		:global(.page-no-banner-navbar #scheme-switch) {
			display: none !important;
		}

		:global(.page-no-banner-navbar #nav-menu-switch) {
			position: absolute;
			top: 0.75rem;
			right: 1rem;
			z-index: 1;
			display: inline-flex !important;
		}

		.policy-page {
			padding-inline: 0;
		}

		.policy-hero {
			min-height: 31rem;
			border-radius: 0.55rem;
		}

		.policy-hero-content {
			padding: 1.25rem 1rem 1.4rem;
		}

		.policy-brand-row {
			align-items: flex-start;
			gap: 0.85rem;
		}

		.policy-game-icon {
			width: 4.75rem;
			height: 4.75rem;
			flex-basis: 4.75rem;
			border-radius: 0.7rem;
		}

		.policy-hero h1 {
			font-size: 1.95rem;
		}

		.policy-hero-summary {
			font-size: 0.9rem;
			line-height: 1.7;
		}

		.policy-hero-meta {
			align-items: flex-start;
			flex-direction: column;
			gap: 0.55rem;
		}

		.policy-hero-meta > span {
			padding-right: 0;
			border-right: 0;
		}

		.policy-lead {
			grid-template-columns: 1fr;
			gap: 0.7rem;
			padding: 1.25rem 0;
		}

		.policy-lead h2 {
			font-size: 1.24rem;
		}

		.policy-document-layout {
			margin-top: 1.4rem;
		}

		.policy-document {
			padding: 0;
		}

		.policy-section h2 {
			font-size: 1.25rem;
		}

		.policy-list li {
			grid-template-columns: 1fr;
			gap: 0.15rem;
		}

		.policy-effective-row {
			align-items: flex-start;
			flex-direction: column;
			gap: 0.25rem;
		}
	}

	@media print {
		.policy-page {
			max-width: none;
			padding: 0;
		}

		.policy-hero {
			min-height: auto;
			border: 1px solid #bbb;
			box-shadow: none;
		}

		.policy-hero-image,
		.policy-hero-shade,
		.policy-index,
		.policy-print-button,
		.policy-back-link,
		.policy-page-footer {
			display: none;
		}

		.policy-hero-content {
			padding: 1.5rem;
			color: #111;
		}

		.policy-eyebrow,
		.policy-hero-summary,
		.policy-hero-meta {
			color: #444;
		}

		.policy-document-layout {
			display: block;
		}

		.policy-section {
			break-inside: avoid;
		}
	}
</style>

<style is:global>
	.page-no-banner {
		top: 5.5rem !important;
	}

	.page-no-banner #main-grid {
		transform: none !important;
	}

	.page-no-banner-navbar {
		height: 4.5rem !important;
	}
</style>
